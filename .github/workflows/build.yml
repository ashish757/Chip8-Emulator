name: Build & Release CHIP-8 Emulator

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:


permissions:
  contents: write

jobs:
  build-mac:
    runs-on: macos-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v4

      - name: Install Raylib
        run: brew install raylib

      - name: Compile Mac Version
        run: g++ -std=c++17 main.cpp chip8.cpp platform.cpp -o chip8-mac -I$(brew --prefix)/include -L$(brew --prefix)/lib -lraylib

      - name: Create ZIP Archive
        run: |
          mkdir -p Chip8-Mac-release
          mv chip8-mac Chip8-Mac-release/
          cp font.ttf Chip8-Mac-release/
          cp -r bin Chip8-Mac-release/
          zip -r Chip8-Mac.zip Chip8-Mac-release/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mac-zip
          path: Chip8-Mac.zip

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v4

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          update: true
          # We removed MSYS2's raylib and added standard 'make'
          install: mingw-w64-x86_64-gcc make zip

      - name: Compile Raylib from Source
        shell: msys2 {0}
        run: |
          git clone --depth 1 https://github.com/raysan5/raylib.git
          cd raylib/src
          make PLATFORM=PLATFORM_DESKTOP

      - name: Compile Windows Version
        shell: msys2 {0}
        # Notice we added -I and -L to point to the source folder, and added -lopengl32
        run: g++ -std=c++17 main.cpp chip8.cpp platform.cpp -o chip8-windows.exe -Iraylib/src -Lraylib/src -static -static-libgcc -static-libstdc++ -lraylib -lopengl32 -lgdi32 -lwinmm -lcomdlg32

      - name: Create ZIP Archive
        shell: msys2 {0}
        run: |
          mkdir -p Chip8-Windows-release
          mv chip8-windows.exe Chip8-Windows-release/
          cp font.ttf Chip8-Windows-release/
          cp -r bin Chip8-Windows-release/
          zip -r Chip8-Windows.zip Chip8-Windows-release/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-zip
          path: Chip8-Windows.zip

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v4

      - name: Install System Headers
        run: sudo apt-get update && sudo apt-get install -y libasound2-dev libx11-dev libxrandr-dev libxi-dev libgl1-mesa-dev libglu1-mesa-dev libxcursor-dev libxinerama-dev libwayland-dev libxkbcommon-dev zip

      - name: Compile and Install Raylib from Source
        run: |
          git clone --depth 1 https://github.com/raysan5/raylib.git
          cd raylib/src
          make PLATFORM=PLATFORM_DESKTOP
          sudo make install

      - name: Compile Linux Version
        run: g++ -std=c++17 main.cpp chip8.cpp platform.cpp -o chip8-linux -lraylib -lGL -lm -lpthread -ldl -lrt -lX11

      - name: Create ZIP Archive
        run: |
          mkdir -p Chip8-Linux-release
          mv chip8-linux Chip8-Linux-release/
          cp font.ttf Chip8-Linux-release/
          cp -r bin Chip8-Linux-release/
          zip -r Chip8-Linux.zip Chip8-Linux-release/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-zip
          path: Chip8-Linux.zip

  publish-release:
    name: Publish to GitHub Releases
    needs: [build-mac, build-windows, build-linux]
    runs-on: ubuntu-latest
    steps:
      - name: Download Mac ZIP
        uses: actions/download-artifact@v4
        with:
          name: mac-zip
          path: release-files/

      - name: Download Windows ZIP
        uses: actions/download-artifact@v4
        with:
          name: windows-zip
          path: release-files/

      - name: Download Linux ZIP
        uses: actions/download-artifact@v4
        with:
          name: linux-zip
          path: release-files/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-files/*
          generate_release_notes: true